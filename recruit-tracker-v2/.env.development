# 开发环境配置
# dev 环境 CloudBase ID（如需要独立 dev 环境，创建新的 CloudBase 环境后在此配置）
VITE_CLOUDBASE_ENV_ID=xlc-recruit-d1gmbx8gybc8a3565

# D-2 候选人列表分页下推的一行回滚开关（默认开启）。
# 设为 false 即整体退回 D-1 的全量装配路径——结果不变，只是慢。
# 排查「列表数据不对」时，可先置 false 确认是否与下推有关。
# 另有运行时熔断：下推路径一旦出错会自动降级并停止重试，无需改这里。
# 详见 src/services/candidate-listing.js 的 loadWorkspace 注释。
VITE_LIST_PUSHDOWN=true
