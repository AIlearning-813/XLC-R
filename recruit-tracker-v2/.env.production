# 生产环境配置
#
# 注意：这里**不写** VITE_CLOUDBASE_ENV_ID。
# 环境 ID 由 .env.local（被 gitignore）提供；若在此重复写死，其优先级高于 .env.local，
# 会把「本地指向别的环境」这一用法直接覆盖掉。
# scripts/deploy.sh 也是从 .env.local 读环境 ID 的，两边口径一致。

# D-2 候选人列表分页下推的**显式**开关。
# 代码里的默认值虽然已是「开启」（VITE_LIST_PUSHDOWN 未定义即开启），但生产构建
# 不该依赖「未定义」这种隐式默认——漏配和显式关闭在产物里长得一模一样，排查时无从分辨。
# 故在此写死，让生产开关是确定的、可审计的。
#
# 回滚：改成 false → npm run build → tcb hosting deploy dist/ -e xlc-recruit-d1gmbx8gybc8a3565
# 结果与 D-1 完全一致，只是慢。运行时熔断（下推出错自动降级）不依赖这里。
VITE_LIST_PUSHDOWN=true
